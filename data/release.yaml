---
remote_endpoint: http://fullnode.mainnet.aptoslabs.com
# replace with below for actual release, compat test needs concrete URL above:
# remote_endpoint: ~
name: "vX.YY.Z"
proposals:
  - name: proposal_1_deploy_topo_poc
    metadata:
      title: "Deploy a new contract to address 0x5""
      description: "This includes changes in https://github.com/aptos-labs/aptos-core/releases/tag/aptos-node-vX.YY.Z"
    execution_mode: MultiStep
    update_sequence:
      - Gas:
          new: current
      - Framework:
          bytecode_version: 8
          git_hash: ~